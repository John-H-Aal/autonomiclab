---
phase: 02-users-db-token-split
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/release.yml
autonomous: true
requirements:
  - AUTH-03

must_haves:
  truths:
    - "release.yml passes USERS_DB_READ_TOKEN secret to iscc, not USERS_DB_TOKEN"
    - "installer.iss config-write block contains no users_db_admin_token line"
  artifacts:
    - path: .github/workflows/release.yml
      provides: updated secret reference
      contains: USERS_DB_READ_TOKEN
    - path: installer.iss
      provides: verified config-write block
      contains: "users_db_token: \"{#UsersDbToken}\""
  key_links:
    - from: .github/workflows/release.yml
      to: installer.iss /DUsersDbToken flag
      via: "$token = \"${{ secrets.USERS_DB_READ_TOKEN }}\""
      pattern: "USERS_DB_READ_TOKEN"
---

<objective>
Rename the GitHub Actions secret reference from USERS_DB_TOKEN to USERS_DB_READ_TOKEN in the iscc build step. Verify installer.iss has no users_db_admin_token line in its config-write block (expected: already absent).

Purpose: The secret that gets embedded in every installer is explicitly scoped to read-only. The old combined-scope secret name is no longer referenced in CI.
Output: Updated release.yml. No change to installer.iss (verification only).
</objective>

<execution_context>
@/home/john/.claude/get-shit-done/workflows/execute-plan.md
@/home/john/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/john/Projects/Python/AutonomicLab/.planning/ROADMAP.md
@/home/john/Projects/Python/AutonomicLab/.planning/STATE.md
@/home/john/Projects/Python/AutonomicLab/.planning/phases/02-users-db-token-split/02-CONTEXT.md

<interfaces>
<!-- Relevant section of .github/workflows/release.yml (lines 93-101): -->
```yaml
      - name: Build installer
        run: |
          $version = "${{ github.ref_name }}".TrimStart('v')
          $password = "${{ secrets.INSTALLER_PASSWORD }}"
          $token = "${{ secrets.USERS_DB_TOKEN }}"
          if ($password) {
            iscc /DAppVersion=$version /DAppPassword=$password /DUsersDbToken=$token installer.iss
          } else {
            iscc /DAppVersion=$version /DUsersDbToken=$token installer.iss
          }
```
Change: secrets.USERS_DB_TOKEN → secrets.USERS_DB_READ_TOKEN (line 96 only)

<!-- installer.iss config-write block (lines 73-81) — VERIFY no admin token line present: -->
```pascal
    Lines[0] := '# AutonomicLab configuration';
    Lines[1] := '# Edit data_folder to match the location of your Finapres data';
    Lines[2] := '';
    Lines[3] := 'data_folder: "' + DocsPath + '"';
    Lines[4] := '';
    Lines[5] := 'users_db_token: "{#UsersDbToken}"';
    Lines[6] := 'allow_guest: true';
    SaveStringsToUTF8File(ConfigFile, Lines, False);
```
Expected: no users_db_admin_token line. SetArrayLength(Lines, 7) — 7 entries, none is admin_token. No change needed.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename USERS_DB_TOKEN to USERS_DB_READ_TOKEN in release.yml</name>
  <files>.github/workflows/release.yml</files>
  <read_first>
    - .github/workflows/release.yml
    - .planning/phases/02-users-db-token-split/02-CONTEXT.md (Decision C)
  </read_first>
  <action>
    In the "Build installer" step (line 96), change:

    ```yaml
          $token = "${{ secrets.USERS_DB_TOKEN }}"
    ```

    to:

    ```yaml
          $token = "${{ secrets.USERS_DB_READ_TOKEN }}"
    ```

    Single occurrence. The /DUsersDbToken flag name on the iscc invocation lines stays unchanged — that is Inno Setup's internal define name, not the GitHub secret name.

    No other changes to release.yml.
  </action>
  <verify>
    <automated>cd /home/john/Projects/Python/AutonomicLab && grep -c "USERS_DB_READ_TOKEN" .github/workflows/release.yml</automated>
  </verify>
  <acceptance_criteria>
    grep returns 1.
    The remaining string "USERS_DB_TOKEN" (without READ_) does NOT appear in release.yml:
      grep -c '"USERS_DB_TOKEN"' .github/workflows/release.yml → 0
    The /DUsersDbToken flag is still present on the iscc lines.
  </acceptance_criteria>
  <done>release.yml references secrets.USERS_DB_READ_TOKEN; secrets.USERS_DB_TOKEN is gone from the file.</done>
</task>

<task type="auto">
  <name>Task 2: Verify installer.iss has no users_db_admin_token line</name>
  <files>installer.iss</files>
  <read_first>
    - installer.iss
    - .planning/phases/02-users-db-token-split/02-CONTEXT.md (Decision A: installer MUST NOT write admin token)
  </read_first>
  <action>
    Read installer.iss and confirm the [Code] section's config-write block (CurStepChanged procedure) does NOT contain any reference to users_db_admin_token.

    Expected state: SetArrayLength(Lines, 7) with Lines[0..6] containing only:
    - comment lines
    - data_folder line
    - users_db_token line
    - allow_guest line

    No users_db_admin_token entry exists. If this is already the case, no edit is needed — record "verified absent" in the summary.

    If (unexpectedly) a users_db_admin_token line IS present, remove it and adjust SetArrayLength accordingly.
  </action>
  <verify>
    <automated>cd /home/john/Projects/Python/AutonomicLab && grep -c "users_db_admin_token" installer.iss</automated>
  </verify>
  <acceptance_criteria>
    grep returns 0.
    installer.iss config-write block writes exactly: data_folder, users_db_token, allow_guest — no admin token.
  </acceptance_criteria>
  <done>installer.iss confirmed: admin token absent from shipped config.yaml template. AUTH-03 satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions secrets → iscc build | Secret value embedded in installer at build time; scope matters |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Elevation of Privilege | USERS_DB_TOKEN (write-capable) embedded in installer | mitigate | Replace with USERS_DB_READ_TOKEN (read-only PAT) per AUTH-03; old secret deleted after verified release |
| T-02-05 | Information Disclosure | Read-only PAT in installed config.yaml | accept | Read-only PAT has no write capability; exposure limited to Contents:read on one private repo |
</threat_model>

<verification>
```bash
cd /home/john/Projects/Python/AutonomicLab
# New secret name present
grep "USERS_DB_READ_TOKEN" .github/workflows/release.yml
# Old secret name gone
grep "USERS_DB_TOKEN" .github/workflows/release.yml
# (must return only lines containing USERS_DB_READ_TOKEN — none with bare USERS_DB_TOKEN)
# installer.iss has no admin token
grep "users_db_admin_token" installer.iss
# (must return empty)
```

Pre-merge ops reminder (not automated — for human verification before tag push):
- Create GitHub Actions secret USERS_DB_READ_TOKEN with the read-only PAT value
- Do NOT delete USERS_DB_TOKEN until first release with split tokens is verified working
</verification>

<success_criteria>
- release.yml: secrets.USERS_DB_READ_TOKEN is the only token secret reference in the iscc step
- installer.iss: config-write block writes users_db_token only (no admin token)
- CI will fail gracefully (empty token) if USERS_DB_READ_TOKEN secret not yet created — address before tagging
</success_criteria>

<output>
After completion, create `/home/john/Projects/Python/AutonomicLab/.planning/phases/02-users-db-token-split/02-02-SUMMARY.md`
</output>
