---
phase: 02-users-db-token-split
plan: 03
type: execute
wave: 2
depends_on:
  - 02-01
  - 02-02
files_modified:
  - CLAUDE.md
  - docs/user_guide.md
autonomous: true
requirements:
  - AUTH-05

must_haves:
  truths:
    - "CLAUDE.md auth section describes two-token model: users_db_token (read-only, shipped) and users_db_admin_token (write, admin-manual)"
    - "user_guide.md admin section explains that admin machines need users_db_admin_token in config.yaml"
    - "AUTH-05 rotation checklist is captured in the plan verification block"
  artifacts:
    - path: CLAUDE.md
      provides: updated auth section
      contains: users_db_admin_token
    - path: docs/user_guide.md
      provides: updated admin section
      contains: users_db_admin_token
  key_links:
    - from: CLAUDE.md
      to: two-token model description
      via: auth section prose
      pattern: "users_db_admin_token"
    - from: docs/user_guide.md
      to: admin config instructions
      via: admin section / How the user list is shared
      pattern: "users_db_admin_token"
---

<objective>
Update CLAUDE.md auth section and user_guide.md admin section to describe the two-token model that ships with this phase. Both docs were last edited in Phase 1 to describe the pre-split (single-PAT) state; they now need the new reality.

Purpose: A fresh Claude session reading CLAUDE.md gets accurate context. Admins reading user_guide.md know they need to manually add the admin token.
Output: Updated CLAUDE.md, updated docs/user_guide.md.
</objective>

<execution_context>
@/home/john/.claude/get-shit-done/workflows/execute-plan.md
@/home/john/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/john/Projects/Python/AutonomicLab/.planning/ROADMAP.md
@/home/john/Projects/Python/AutonomicLab/.planning/STATE.md
@/home/john/Projects/Python/AutonomicLab/.planning/phases/02-users-db-token-split/02-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-edit CLAUDE.md auth section for two-token model</name>
  <files>CLAUDE.md</files>
  <read_first>
    - CLAUDE.md
    - .planning/phases/02-users-db-token-split/02-CONTEXT.md (Full Change Map, "Also updates CLAUDE.md…")
  </read_first>
  <action>
    Find the `## Auth system` section in CLAUDE.md. Replace the paragraph that describes the PAT/token with a two-token description. The key facts to convey:

    - `users_db_token` — read-only PAT (Contents: read on `autonomiclab-users`); embedded by `installer.iss` from CI secret `USERS_DB_READ_TOKEN`; used only by `sync_users_db()` on every launch
    - `users_db_admin_token` — write PAT; NOT shipped in installer; admin adds manually to `config.yaml` on their machine only; used only by `push_users_db()` via Admin Panel close
    - If `users_db_admin_token` is absent, Admin Panel close shows a QMessageBox.warning ("Sync not configured") instead of silently skipping

    The existing auth section (from Phase 1 edit) currently reads:
    > The PAT used for the call lives in `config.yaml` under `users_db_token` (currently a single combined-scope PAT shipped in every installer; embedded by `installer.iss` from CI secret `USERS_DB_TOKEN`).

    Replace that sentence and any adjacent single-PAT language with the two-token model. Keep all other auth-section content intact (roles, users.db description, first-run bypass, Admin Panel visibility, PAT = currently combined-scope note can be removed).

    Write style: terse, factual, present-tense. No comments. Follow existing CLAUDE.md paragraph style.

    Target prose for the PAT paragraph (adapt as needed to fit surrounding context):
    ```
    Two PATs are used:
    - `users_db_token` — read-only (Contents: read); shipped in every installer via CI secret `USERS_DB_READ_TOKEN`; used only by `sync_users_db()` on launch.
    - `users_db_admin_token` — write-capable; NOT shipped; admin adds manually to `config.yaml` on their machine. Used only by `push_users_db()` when Admin Panel closes. If absent, Admin Panel close shows a "Sync not configured" warning instead of pushing.
    ```
  </action>
  <verify>
    <automated>cd /home/john/Projects/Python/AutonomicLab && grep -c "users_db_admin_token" CLAUDE.md</automated>
  </verify>
  <acceptance_criteria>
    grep returns at least 1.
    CLAUDE.md auth section contains "users_db_admin_token".
    CLAUDE.md auth section contains "USERS_DB_READ_TOKEN".
    The phrase "currently a single combined-scope PAT" does not appear.
    The phrase "USERS_DB_TOKEN" (without READ_) does not appear in CLAUDE.md.
  </acceptance_criteria>
  <done>CLAUDE.md auth section accurately describes the two-token model as shipped.</done>
</task>

<task type="auto">
  <name>Task 2: Add admin token setup note to user_guide.md</name>
  <files>docs/user_guide.md</files>
  <read_first>
    - docs/user_guide.md
    - .planning/phases/02-users-db-token-split/02-CONTEXT.md (Full Change Map)
  </read_first>
  <action>
    Find the "### How the user list is shared" section (around line 172). This section currently describes sync and push as if a single token does both.

    Update the section to reflect the two-token model:

    1. In the description of "When an administrator closes the Admin Panel" (step 2: "The local users.db is uploaded back to GitHub"), add a note that this requires the admin token to be configured.

    2. After the existing failure paragraph ("If the upload fails..."), add a new paragraph explaining the admin token setup:

    ```markdown
    **Admin machine setup (one-time):**

    To enable syncing from the Admin Panel, add the following line to `config.yaml` next to `AutonomicLab.exe` on the admin's machine:

    ```yaml
    users_db_admin_token: "ghp_your_write_token_here"
    ```

    Contact the system administrator (John Hansen) to obtain the write token. This token is not included in the standard installer — it is only added manually on machines used for admin tasks. Other users' installations are unaffected.
    ```

    Keep all other content in this section intact. Do not alter the "First run" or "Disabling guest access" subsections.
  </action>
  <verify>
    <automated>cd /home/john/Projects/Python/AutonomicLab && grep -c "users_db_admin_token" docs/user_guide.md</automated>
  </verify>
  <acceptance_criteria>
    grep returns at least 1.
    docs/user_guide.md contains "users_db_admin_token".
    The config.yaml example block in user_guide.md shows the users_db_admin_token key.
    The "First run" and "Disabling guest access" subsections are unchanged.
  </acceptance_criteria>
  <done>user_guide.md admin section instructs admins to manually add users_db_admin_token to their config.yaml.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs → admin operator | Documentation must accurately describe which token is needed to avoid misconfiguration |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06 | Information Disclosure | user_guide.md admin token instructions visible to non-admins | accept | user_guide is a public document; token value is not included, only the config key name |
</threat_model>

<verification>
```bash
cd /home/john/Projects/Python/AutonomicLab
# CLAUDE.md two-token model present
grep "users_db_admin_token" CLAUDE.md
grep "USERS_DB_READ_TOKEN" CLAUDE.md
# Old single-PAT language gone
grep -v '^#' CLAUDE.md | grep -c "USERS_DB_TOKEN"
# (expected 0 — no bare USERS_DB_TOKEN references in non-comment lines)

# user_guide.md admin token setup present
grep "users_db_admin_token" docs/user_guide.md
```

AUTH-05 PAT rotation checklist (ops steps — no code change, human action required):

1. Create fine-grained PAT on GitHub (Settings → Developer settings → Fine-grained tokens):
   - Resource owner: John-H-Aal
   - Repository access: Only `autonomiclab-users`
   - Permissions: Contents = Read
   - Name: autonomiclab-read

2. In the autonomiclab repo Actions settings (Settings → Secrets → Actions):
   - Add secret: USERS_DB_READ_TOKEN = <new read-only PAT>
   - Keep USERS_DB_TOKEN until verified

3. Push a version tag to trigger a release build with USERS_DB_READ_TOKEN.

4. Install the new release. Verify:
   - `sync_users_db()` works on launch (reads users.db from GitHub)
   - `push_users_db()` returns False / HTTP 403 when using the embedded token (read-only PAT cannot PUT)

5. On the admin machine, add `users_db_admin_token` to config.yaml with the write-capable PAT.
   Verify Admin Panel close pushes successfully.

6. After one verified release cycle:
   - Revoke the old combined-scope PAT on GitHub (Developer settings → Personal access tokens → delete)
   - Delete the USERS_DB_TOKEN secret from Actions settings
   - Update the local dev-tree config.yaml (gitignored) if it contains the old PAT value
</verification>

<success_criteria>
- CLAUDE.md: two-token model described, USERS_DB_READ_TOKEN named, users_db_admin_token named, old single-PAT language removed
- user_guide.md: admin token config step documented under "How the user list is shared"
- AUTH-05 rotation checklist captured in verification block above (human-executed post-release)
</success_criteria>

<output>
After completion, create `/home/john/Projects/Python/AutonomicLab/.planning/phases/02-users-db-token-split/02-03-SUMMARY.md`
</output>
